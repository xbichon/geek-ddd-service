## 学生模块
### 功能
1. 创建学生
2. 更新学生
3. 删除学生
4. 查询学生
5. 导出学生
6. 导入学生
## 概念
1. 学生
    学生姓名
    学生学号
    学生性别
    学生年龄
    学生手机
    学生邮箱
    班级ID
    班级名称
    学生家庭住址
    学生身份证号
    学生家庭电话
2. 班级


只有学生概念（其他的先不管）
技术

设计点：
- 高内聚、松耦合
- 代码更容易维护
- 分而治之


## 基础框架
adapter 适配器层
application 应用层
    - command  (命令) 写操作（业务逻辑）   （MQ）排队... (压力) 技术栈
          业务规则、协作流程.....  复杂

    - query   （查询） 读操作（界面展示、报表、导出~~~~） （查询做优化） 缓存、搜索引擎...
          简单，联表查询、

CPU、磁盘 《-（ 做好限制）
    MQ -》  =========  CPU
    数据库（单例） 《-》 内存 / es(搜索引擎)
    数据库（1写） ->   10 读的库


  业务上、技术栈 （两个不同的方向）


    学生姓名
    学生学号
    学生性别（0：男、1：女、-1：未知）
    学生年龄
    学生手机
    学生邮箱
    班级ID
    班级名称

联表查询
数据模型

设计模式 -> 遵循设计原则 -> 单一职责；


     CQRS 读写分离
domain 领域层


## 建立模型

JDBC -> SQL 语句 （很难维护、切换数据库很麻烦） 写起来很麻烦


     （不想在这写JDBC）-解决方案（来一层映射）
对象  ——》  表   -> JPA - > ORM  对象关系映射 ...  (气宗)
jAVA   翻译   SQL
Object  Mapper   关系数据库  （ORM）
 
JDBC Helper -》XML-》 Mybatis  （剑宗） 目标方便写SQL
(半自动ORM)

DDD 面向对象  Code -》 数据库  code first
             数据库 -》 代码   db first



模型 -》 聚合 （聚合根）  业务概念（名次） ID
领域服务 -》   协作问题
值对象 -》  int time ...  复杂业务系统 邮箱（string、 zhangsan@163.com)
       家庭住址（省、市、县）ID
仓库：


柜员（概念）、业务系统、存单...  (概念)


操作手册（流程、规章）
1.先收到存单
2.根据存单操作业务系统（规则1、规则2）
3.柜员.....


JPA -> 根据模型自动生成数据库（关掉） 适合做小项目
- > liquibase (自动生成表)
  > 
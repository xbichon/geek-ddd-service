spring:
  datasource:
    url: ${config.database.url}
    username: ${config.database.username}
    password: ${config.database.password}
    driver-class-name: ${config.database.driver-name}
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: true
    properties:
      hibernate:
        default_schema: ${config.database.name}

server:
  port: 8090

logging:
  level:
    vip.geekclub: DEBUG

config:
  database:
    driver-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useSSL=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 12345678
    name: test
  # JWT配置
  jwt:
    secret-key: ${JWT_SECRET_KEY:Geek2024SecurityDianComChina12987T23001Y-Q8Y05OOD}
    expiration-days: ${JWT_EXPIRATION_DAYS:7}
    issuer: geekclub.vip
  # 微信小程序配置
  wechat:
    mini-program:
      app-id: ${WECHAT_APP_ID:wx4169de1d28757b63}
      app-secret: ${WECHAT_APP_SECRET:630ce71078cbc6d87f1e96efe0b4b09d}
      api-base-url: ${WECHAT_API_BASE_URL:https://api.weixin.qq.com}
      jscode2session-url: ${WECHAT_JSCODE2SESSION_URL:/sns/jscode2session}